我们在数据库操作的时候，比如 dao 层中当遇到一个 sql.ErrNoRows 的时候，是否应该 Wrap 这个 error，抛给上层。为什么，应该怎么做请写出代码？
不应该，只有应用使用wrap error策略，高可重用性的包返回根错误值
err = dao.Query(sql)
if err != nil {
    if err == sql.ErrNoRows {
        return err
    } else {
        ...
    }
}